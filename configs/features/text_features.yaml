text_features:
  method: "sbert"  # options: tfidf, sbert, bert
  model_name: "sentence-transformers/all-MiniLM-L6-v2"
  max_length: 128
  cache_path: "data/processed/text_embeddings.joblib"
  normalize: true
