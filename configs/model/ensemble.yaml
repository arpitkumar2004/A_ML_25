ensemble:
  method: "stacking"  # options: stacking, averaging
  base_models: ["Linear", "RF", "LGBM", "XGB", "Cat"]
  meta_model: "ridge"  # or "lgbm"
  meta_params:
    alpha: 1.0
  n_splits: 5
  random_state: 42
