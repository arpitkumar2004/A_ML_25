model:
  type: "fusion_nn"
  image_encoder: "clip"
  text_encoder: "bert"
  numeric_input_dim: 10   # depends on numeric features
  hidden_dims: [512, 256, 128]
  output_dim: 1
  activation: "relu"
  dropout: 0.2
  batch_size: 32
  learning_rate: 1e-3
  epochs: 20
  device: "cuda"
